<%= form_with(model: birthday_message) do |form| %>
  <% if birthday_message.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(birthday_message.errors.count, "error") %> prohibited this birthday_message from being saved:</h2>

      <ul>
        <% birthday_message.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
